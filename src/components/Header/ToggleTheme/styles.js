import styled from "styled-components"

export const Wrapper = styled.button`
  background: none;
  border: none;
  cursor: pointer;
  margin: 0 10px;

  &:focus {
    outline: none;
  }

  img {
    margin-bottom: unset;
  }
`
